<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Donation Approval</a>
            <div class="collapse navbar-collapse">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link active" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/test">Test Page</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/settings">Settings</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <div class="container">
        <h1>Export Settings</h1>
        <form action="/settings" method="POST">
            <div class="mb-3">
                <label for="exportFilePath" class="form-label">Export File Path</label>
                <input type="text" class="form-control" id="exportFilePath" name="exportFilePath" value="<%= exportSettings.filePath %>" required>
            </div>
            <div class="mb-3">
                <label for="exportFileName" class="form-label">Export File Name</label>
                <input type="text" class="form-control" id="exportFileName" name="exportFileName" value="<%= exportSettings.fileName %>" required>
            </div>
            <div class="mb-3">
                <label for="exportFormat" class="form-label">Export Format</label>
                <select class="form-control" id="exportFormat" name="exportFormat" required>
                    <option value="csv" <%= exportSettings.exportFormat === 'csv' ? 'selected' : '' %>>CSV</option>
                    <option value="excel" <%= exportSettings.exportFormat === 'excel' ? 'selected' : '' %>>Excel</option>
                </select>
            </div>
            <button type="submit" class="btn btn-primary">Save Settings</button>
        </form>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
